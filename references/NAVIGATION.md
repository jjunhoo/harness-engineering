# 저장소 탐색 가이드

1. **최상위**: `AGENTS.md` → `DESIGN.md`
2. **작업 중**: `plans/` 해당 파일 → 연결된 `docs/`·`architecture/`
3. **품질**: `evaluations/README.md` → `rubrics/`·`checklists/`
4. **자동화**: `.github/workflows/` + `scripts/`

## 검색 힌트

- “왜 이렇게 했지?” → `architecture/adr/`
- “무엇을 측정하지?” → `QUALITY_SCORE.md`, `evaluations/rubrics/`
- “머지 전 필수?” → `rules/`, CI 워크플로
